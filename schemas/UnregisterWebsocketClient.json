{
    "id": "UnregisterWebsocketClient",
    "description": "Request to (forcefully) unregister a websocket client",
    "properties": {
        "jwt": {
            "type": "string"
        },
        "userId": {
            "type": "string"
        }
    },
    "oneOf": [
        {
            "required": [
                "jwt"
            ]
        },
        {
            "required": [
                "userId"
            ]
        }
    ]
}